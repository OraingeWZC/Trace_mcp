# 基于轨迹的故障分类（MCP 工具说明）

> 本文档参考《日志解析（@刘玉河）》的写法风格，先给出两个“轨迹故障分类”相关工具的说明：
>
> - **轨迹故障分类工具（SV）**：仅基于 Trace 结构进行故障分类
> - **轨迹拓扑融合故障分类工具（SVND）**：融合 Trace +（节点/拓扑/上下文）信息进行故障分类
>
> 说明：本文描述的是 **MCP 工具层接口**（也就是你在 `npx @modelcontextprotocol/inspector python -m app.main` 里看到的 tools）。

---

## 工具说明

Trace_mcp 提供一组基于轨迹（Trace/Span）的故障分类 MCP 工具。它们把 `app/tools/` 下的训练/评估脚本封装为可被 MCP Client（Inspector / LLM Agent）调用的工具，并统一：

- 工具注册与 schema（`app/main.py`）
- 子进程执行、日志落盘、返回 tail（`app/handler.py`）
- 配置控参（对 gtrace 类工具提供 `get_gtrace_config` / `update_gtrace_config`）

本文件聚焦于 **“轨迹故障分类（SV）”** 与 **“轨迹+拓扑融合故障分类（SVND）”** 两类工具。

---

## 核心能力

| 能力 | 说明 | 对应工具 |
|---|---|---|
| 数据预处理 | 将原始轨迹数据转换为训练/评估可用的结构化数据 | `preprocess_aiops_sv` / `preprocess_aiops_svnd` |
| 模型训练 | 基于训练集训练故障分类模型（不同任务粒度/多头分类等） | `train_aiops_sv` / `train_aiops_svnd` |
| 模型测试/评估 | 在测试集上输出分类效果与日志（建议设置 limit 防超时） | `test_aiops_sv` / `test_aiops_svnd` |
| 统一日志落盘 | 每次执行生成 `mcp_<tool_id>_<stage>_<timestamp>.log` | 所有脚本型工具 |
| 通用执行入口 | 用统一方式指定 tool_id + stage + args 执行底层脚本/模块 | `run_trace_tool` |

---

## 通用数据结构与字段说明

> 本节描述 MCP 工具**通用返回结构**。底层模型/数据的“领域字段”由各自工具脚本决定。

### 1. ScriptResult（子进程执行结果）

| 字段名 | 类型 | 说明 |
|---|---|---|
| `exit_code` | integer | 子进程退出码（0 表示成功） |
| `log` | string | 仅返回日志尾部（tail），避免响应过大 |
| `command` | string | 实际执行的命令行（含 python 路径与参数） |
| `cwd` | string | 子进程工作目录（通常为对应工具目录） |
| `log_path` | string | 完整日志文件路径（建议优先看它排错） |

---

## 通用返回格式

每个工具返回一个 JSON-like Dict（Inspector 中直接展示为 JSON），通常包含：

| 字段 | 说明 |
|---|---|
| `exit_code` | 0 表示成功，非 0 表示失败 |
| `log` | 输出日志 tail（失败时会包含 argparse/traceback 等信息） |
| `log_path` | 完整日志文件路径（排查错误以它为准） |

当工具失败时，MCP 层会抛出异常（Inspector 会显示 `Error executing tool ...`），其中包含 `exit_code` 与 `log_path`。

---

## 工具列表（与本文相关）

| MCP 工具名 | 作用 | 典型使用 |
|---|---|---|
| `preprocess_aiops_sv` | SV：预处理 | 生成 SV 训练/测试所需数据 |
| `train_aiops_sv` | SV：训练 | 训练 SV 故障分类模型 |
| `test_aiops_sv` | SV：评估 | 输出 SV 分类评估日志 |
| `preprocess_aiops_svnd` | SVND：预处理 | 生成 SVND 训练/测试所需数据 |
| `train_aiops_svnd` | SVND：训练 | 训练 SVND 多头分类模型 |
| `test_aiops_svnd` | SVND：评估 | 输出 SVND 分类评估日志 |
| `run_trace_tool` | 通用入口 | 推荐用于参数较复杂/脚本参数名不统一的场景 |

---

## 轨迹故障分类工具（SV）

### 工具概述

SV（Single Trace）工具用于在**仅使用轨迹结构信息**（Trace/Span 调用结构）时进行故障分类。适用于不依赖节点拓扑/主机指标的场景，强调“轨迹结构异常”与“服务调用关系”。

### 对应底层脚本

| stage | 脚本 | 目录 |
|---|---|---|
| preprocess | `make_aiops_sv.py` | `Trace_mcp/app/tools/trace_sv_diag/` |
| train | `train_aiops_sv.py` | `Trace_mcp/app/tools/trace_sv_diag/` |
| test | `test_aiops_sv.py` | `Trace_mcp/app/tools/trace_sv_diag/` |

### test_aiops_sv（评估）

#### 功能描述

对 SV 模型进行评估（故障分类），输出评估日志与可能的结果文件（以脚本实现为准）。

#### 参数说明（推荐）

> 注意：SV/SVND 这类脚本的 argparse 参数名有的使用下划线（`--data_root`），有的使用短横线（`--data-root`）。  
> 如果你在 Inspector 里看到 `unrecognized arguments`，请优先使用本文的 **“通用入口 run_trace_tool + _raw_args”** 方式。

| 参数名 | 类型 | 必填 | 说明 |
|---|---:|---:|---|
| `model_path` | string | 否 | 模型权重路径（默认值由工具封装提供） |
| `data_root` | string | 否 | 数据根目录 |
| `task` | string | 否 | 任务粒度（如 `fine/superfine`，以脚本为准） |
| `batch` | integer | 否 | batch size |
| `device` | string | 否 | `cpu` / `cuda` |
| `seed` | integer | 否 | 随机种子 |
| `limit` | integer | 否 | 限制评估样本量（强烈建议用于 MCP 调试） |

#### 返回值

返回通用 `ScriptResult`。

#### 使用示例（推荐：通用入口，避免参数名不匹配）

```json
{
  "tool_id": "aiops_sv",
  "stage": "test",
  "args": {
    "_raw_args": [
      "--model_path", "dataset/aiops_sv/aiops_superfine_cls.pth",
      "--data_root", "dataset/aiops_sv",
      "--task", "superfine",
      "--batch", "64",
      "--device", "cuda",
      "--seed", "2025",
      "--limit", "50"
    ]
  },
  "timeout_sec": 600,
  "tail_lines": 200
}
```

---

## 轨迹拓扑融合故障分类工具（SVND）

### 工具概述

SVND（Trace + Node/Deployment/Topology Fusion）工具用于在故障分类时融合：

- Trace 调用链结构
- 节点/实例信息（node/service/pod 等）
- 可能的上下文窗口/拓扑特征（以脚本实现为准）

适用于“单纯看 Trace 不够”的复杂场景，例如需要区分服务故障与节点故障、或需要主机/拓扑注入的场景。

### 对应底层脚本

| stage | 脚本 | 目录 |
|---|---|---|
| preprocess | `make_aiops_svnd.py` | `Trace_mcp/app/tools/trace_svnd_diag/` |
| train | `train_aiops_svnd.py` | `Trace_mcp/app/tools/trace_svnd_diag/` |
| test | `test_aiops_svnd.py` | `Trace_mcp/app/tools/trace_svnd_diag/` |
| debug | `debug_check_svnd.py` | `Trace_mcp/app/tools/trace_svnd_diag/` |

### test_aiops_svnd（评估）

#### 功能描述

对 SVND 模型进行评估（多头分类/融合分类），输出评估日志与可能的结果文件（以脚本实现为准）。

#### 参数说明（与脚本一致）

从你近期报错日志可知，SVND 的 `test_aiops_svnd.py` 参数名使用下划线风格，例如：

- `--data_root`
- `--ckpt`
- `--batch_size`
- `--device`
- `--seed`
- `--host_conv {gcn,gat}`
- `--limit`

因此推荐使用 `_raw_args` 以确保参数原样传入。

| 参数名 | 类型 | 必填 | 说明 |
|---|---:|---:|---|
| `ckpt` | string | 是 | 权重文件路径（等价于你希望的 `model_path`） |
| `data_root` | string | 否 | 数据根目录 |
| `batch_size` | integer | 否 | batch size |
| `device` | string | 否 | `cpu` / `cuda` |
| `seed` | integer | 否 | 随机种子 |
| `host_conv` | string | 否 | `gcn` 或 `gat`（如果脚本支持） |
| `limit` | integer | 否 | 限制评估样本量（强烈建议用于 MCP 调试） |

#### 返回值

返回通用 `ScriptResult`。

#### 使用示例（推荐：通用入口 run_trace_tool + _raw_args）

```json
{
  "tool_id": "aiops_svnd",
  "stage": "test",
  "args": {
    "_raw_args": [
      "--ckpt", "dataset/aiops_svnd/1019/aiops_nodectx_multihead.pt",
      "--data_root", "dataset/aiops_svnd",
      "--batch_size", "128",
      "--device", "cuda",
      "--seed", "2025",
      "--host_conv", "gcn",
      "--limit", "50"
    ]
  },
  "timeout_sec": 600,
  "tail_lines": 200
}
```

---

## 常见问题（FAQ）

| 问题 | 典型报错 | 解决方案 |
|---|---|---|
| 参数名不匹配 | `error: unrecognized arguments: --batch-size ...` | 用 `run_trace_tool` 并通过 `args._raw_args` 传入脚本真实参数名（尤其是下划线风格） |
| 超时/日志过大 | 无响应、或输出太长 | 优先设置 `limit`；必要时设置 `timeout_sec`；查看 `log_path` 完整日志 |
| 环境依赖问题 | ImportError / ModuleNotFoundError | 在目标 venv 中安装依赖；远程注意 pydantic v2 与 mcp 的兼容性 |

