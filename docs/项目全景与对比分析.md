# project_3_algo_tools 项目全景与对比分析

> 本目录是国家重点研发计划课题"基于智能分析的云原生系统运行时故障预测、定位和诊断"（项目编号 2024YFB4505903，子课题编号 12100000400000624D--2024YFB4505903/02）的算法工具集成仓库。
> 视角：以"5 个交付指标 × 3 个核心任务（预警/定位/诊断）"切入，定位 wzc 与linyu两人在整个课题中的位置、与其他子团队的差异、应当对齐什么、可以借用什么。
> 状态：仅分析，未改动任何代码。

---

## 0. 课题背景

| 字段 | 内容 |
|---|---|
| 项目类别 | 国家重点研发计划，先进计算与新兴软件专项 |
| 项目编号 | 2024YFB4505900（云原生软件生态系统智能化开发、测试与运维） |
| 课题编号 | 2024YFB4505903（基于智能分析的云原生系统运行时故障预测、定位和诊断） |
| 子课题报告编号 | 12100000400000624D--2024YFB4505903/02 |
| 牵头单位 | 清华大学 |
| 合作单位 | 浙江大学 |
| 课题负责人 | 裴丹（清华） |
| 项目周期 | 2024-12 ~ 2027-11 |
| 总经费 | 400 万（国拨） |
| 项目承担单位 | 阿里云计算有限公司（项目负责人黄涛） |
| 中期报告 | 2026-05-27 已上交 |
| 中期检测 | 2026-04-08 ~ 2026-04-10 在清华大学计算机系完成；测试单位：中国电子技术标准化研究院赛西实验室；测试编号 CESI20260320048W-111；**18 项测试用例全部通过** |

---

## 1. 三大研究方向（中期报告第 1~3 章）

```
研究内容 1   多模态运维数据增强技术（数据层）
              ├── AutoDA-Timeseries（单增强策略，时序特征感知）
              ├── Sentinel（多增强策略搜索，多变量依赖挖掘 MDM）
              └── 跨模态协同增强 / 增强数据融合

研究内容 2   多模态时空推理模型构建（模型层）
              ├── 日志-指标异构融合推理
              └── 轨迹-拓扑关联时空建模          ★ wzc 核心方向

研究内容 3   强泛化性运维能力构建（落地层）       ← 5 个交付指标 + 1 全融合
              ├── 3.1 日志单模态预警/定位/诊断   日志团队
              ├── 3.2 指标单模态预警/定位/诊断   指标团队
              ├── 3.3 轨迹单模态预警/定位/诊断   ★ linyu + wzc
              ├── 3.4 日志+指标融合预警/定位/诊断  日志/指标团队
              ├── 3.5 轨迹+拓扑融合预警/定位/诊断  ★ wzc
              └── 3.6 五种运维数据全融合预警/定位/诊断  OpsAug 团队
```

---

## 2. 团队分工（与你描述一致）

| 团队 | 成员 | 负责的交付指标 | 仓库目录 |
|---|---|---|---|
| **日志/指标 团队**（偏整体） | 多人 | 3.1 日志、3.2 指标、3.4 日志+指标融合、3.6 五种全融合 | `log_tools/` `metric_tools/` `metric_log_tools/` `OpsAug-main/` 等 |
| **轨迹/拓扑 团队**（你们俩） | linyu | 3.3 轨迹单模态 **预警 + 根因定位**（TrUST） | `trace_tools/TrUST/` |
| | **wzc** | 3.3 轨迹单模态 **故障诊断**（trace_sv_diag）+ 整个 3.5 轨迹+拓扑融合（预警/定位/诊断） | `trace_tools/dataprocess/` `trace_tools/trace_sv_diag/` `trace_topo_tools/` 全部 |

**注**：检测报告里把 3.1（日志单模态）和 3.2（指标单模态）合并成"指标 3.1：日志、指标单模态"一个交付指标（6 个测试用例），所以中期检测里实际只有 5 个交付指标编号（3.1~3.5）；中期报告论述时拆成 6 个研究方向（3.1~3.6）。两套编号下面统一为"5 项交付指标"。

---

## 3. 5 项交付指标 × 3 任务全景

### 3.1 项目级矩阵（与赛西实验室检测对齐）

| 指标 | 内容 | 模态 | 层次 | 测试用例 | 谁负责 |
|---|---|---|---|---|---|
| **3.1** | 日志、指标 单模态预警/定位/诊断 | 日志 OR 指标 | 平台/服务/应用 三层 | YS02-001~006（6） | 日志/指标团队 |
| **3.2** | 轨迹 单模态预警/定位/诊断 | 轨迹 | **应用层** | YS02-007~009（3） | ★ linyu + wzc |
| **3.3** | 日志+指标 融合预警/定位/诊断 | 日志+指标 | 平台/服务/应用 三层 | YS02-010~012（3） | 日志/指标团队 |
| **3.4** | 轨迹+拓扑 融合预警/定位/诊断 | 轨迹+拓扑 | **应用层** | YS02-013~015（3） | ★ wzc |
| **3.5** | 5 种数据全融合预警/定位/诊断 | 日志+轨迹+指标+拓扑+部署 | 平台/服务/应用 三层 | YS02-016~018（3） | OpsAug/ART 团队 |

每个指标内部固定按"预警 / 定位 / 诊断"三任务分子测试用例。

### 3.2 各板块算法栈速览

| 板块 | 故障预警 (AD) | 故障定位 (RCA) | 故障诊断 (CLS) | 训练负担 |
|---|---|---|---|---|
| log_tools | DeepLog / LogBERT (per service) | 跨服务 AD 聚合排序 | TF-IDF + LR / LogBERT | 中 |
| metric_tools | BOCPD（无训练） | RobustScorer / N-Sigma（无训练） | LR on RCA-ranks | 几乎无 |
| metric_log_tools | 复用 metric_tools | 复用 metric_tools | 复用 metric_tools | 无 |
| **trace_tools** | **TrUST**（GVAE 双自编码器，linyu） | **TrUST**（柔性聚合 Top-K，linyu） | trace_sv_diag（多头分类，wzc）| 重 |
| **trace_topo_tools** | **TraTopoRca**（GVAE+三通道）| **TraTopoRca**（服务级+主机级对齐）| trace_svnd_diag（含 Host Graph）| 最重 |
| OpsAug/ART | ART 多模态预训练 | ART RCL | ART FT (failure triage) | 最重 |

---

## 4. 项目级评测入口与共享基础设施

| 文件 | 作用 |
|---|---|
| `_eval_common.py` | 共享 CSV 校验、日志获取、故障类别 ML 预测 |
| `eval_report_printer.py` | 中文格式化输出 |
| `benchmark_csv_compat.py` | schema 兼容（problem_id ↔ incident_id） |
| `evaluate_log_tools.py` | 日志板块三任务统一评测 |
| `evaluate_metric_tools.py` | 指标板块三任务统一评测（6 case 子任务也有独立脚本） |
| `evaluate_metric_ad/cls/loc.py` | 指标单任务评测 |
| `evaluate_fused_tools.py` | 日志+指标融合板块三任务评测 |
| `evaluate_fused_ad/cls/loc.py` | 融合单任务评测 |
| `evaluate_opsaug_ad.py` / `evaluate_opsaug_diag.py` | 五模态全融合（OpsAug/ART）评测 |
| `train_fault_classifier_ml.py` | 训 6 类 LR 故障类别分类器（指标板块用） |
| `mcp_opsaug_server.py` | OpsAug MCP stdio server |

**关键观察**：trace 端目前没有 `evaluate_trace_*.py` 顶层评测脚本，但你们团队**有自己的集成测试方案**（见下文检测报告），且已通过赛西实验室认证——因此并不强制要求与上述命名对齐。改造路径见 `范式对比与改造思路.md` 中的"可选优化"。

---

## 5. 中期检测实测结果（CESI20260320048W-111）

> 18 个测试用例全部通过。完整结果保留在赛西实验室出具的检测报告里，本节仅摘录与 wzc / linyu相关的部分。

### 5.1 wzc + linyu负责的部分（指标 3.2 + 指标 3.4，共 6 个用例）

| 用例 | 任务 | 工具 | 入口路径 | 实测指标 |
|---|---|---|---|---|
| YS02-007 | 轨迹单模态 故障预警 | TrUST（**linyu**） | `csv-db.py whole_dataset` → `python3 -m main.models.trust.trust_main` → `infer_raw.py` | **F1=0.9240**（TP=12436 / FP=9 / FN=2036 / TN=14464，Acc=0.9240, P=0.9993, R=0.8593） |
| YS02-008 | 轨迹单模态 故障定位 | TrUST（**linyu**） | 同上（TRUST 模型，确保 ID 映射一致） | **Top-5 Acc=1.0000** |
| YS02-009 | 轨迹单模态 故障诊断 | trace_sv_diag（**wzc**） | `cd trace_tools/trace_sv_diag` → `make_tc_sv.py` → `train_tc_sv.py` → `test_tc_sv.py` | **整体 Acc=0.9795；Weighted P=0.8943, R=0.9104, F1=0.9027**（10 类故障，总样本 6229） |
| YS02-013 | 轨迹+拓扑 故障预警 | TraTopoRca（**wzc**） | `cd trace_topo_tools/TraTopoRca` → `python -m tracegnn.models.gtrace.mymodel_test` | **F1=0.6445**（12000 样本，TP=573 / FP=5 / FN=627 / TN=10795，Acc=0.9473, P=0.9914, R=0.4783） |
| YS02-014 | 轨迹+拓扑 故障定位 | TraTopoRca（**wzc**） | 同上 | **服务级 Top5=0.9933、主机级 Top5=1.0000、混合级 Top5=0.7025**（详见 `dataset/rca/processed/reports/eval_report_*.md`） |
| YS02-015 | 轨迹+拓扑 故障诊断 | trace_svnd_diag（**wzc**） | `cd trace_topo_tools/trace_svnd_diag` → `make_tc_svnd.py` → `train_tc_svnd.py` → `test_tc_svnd.py` | **整体 Acc=0.9445；Weighted P=0.8552, R=0.8232, F1=0.8019**（11 类故障，总样本 8728） |

### 5.2 中期报告里的实验数据（CCF AIOps + 阿里云原生）

**轨迹单模态（3.3 节，linyu TrUST + wzc trace_sv_diag）**：
- CCF AIOps：故障预警 F1=79%，故障定位 Top-5=68%，故障诊断 F1=72%
- 阿里云原生测试环境：故障预警 F1=80%，故障定位 Top-5=85%，故障诊断 F1=72%

**轨迹+拓扑（3.5 节，wzc 全部）**：
- CCF AIOps（约 3 万条 trace）：故障诊断 Weighted P=0.8879 / R=0.8777 / F1=**0.8804**
  - 各类型 F1：network delay 87.89% / code error 99.05% / network loss 93.11% / cpu stress 99.71% / network corrupt 89.29% / memory stress 96.48% / dns error 95.88% / node memory stress 87.67% / node cpu stress 91.46% / node disk fill 81.89%
- 阿里云原生测试环境：故障诊断 F1=80%；4000 条混合 Trace（正常+主机级+服务级）：故障预警 F1=64%，故障定位 Top-5=70%

---

## 6. wzc 核心方向的算法定位（中期报告原文）

### 6.1 轨迹单模态（3.3 节）— linyu TrUST + wzc trace_sv_diag 共同支撑

**核心创新**："基于调用路径图-时延"的双自编码器统一框架

- **结构子网络**：GNN 编码 + GNN 解码，重建调用图节点特征 + 邻域分布 → 检测结构异常
- **时序子网络**：MLP 编码 + 树结构 LSTM 解码 → 检测时延异常
- **三大评分优化**：
  - Bernoulli-Categorical Scaling（伯努利-分类缩放）：放大结构异常贡献
  - Gaussian Standard Deviation Constraint（高斯标准差限制）：防止熵逃逸
  - Node Count Normalization（节点计数归一化）：消除轨迹长度系统性偏差
- **柔性聚合**：Flexible Max-Normalization Aggregation（柔性最大值归一化）做服务级聚合
- **联合优化**：Uncertainty-Aware Adaptive Joint Optimization（不确定性感知自适应联合优化），自动平衡两个任务损失权重

### 6.2 轨迹+拓扑融合（3.5 节）— wzc 完全负责

**核心创新**：三通道并行编码 + GVAE 重构

```
分布式追踪数据 + 物理节点指标
        ↓
    [拓扑融合向量] [窗口上下文]
        ↓
┌──────────┬──────────┬──────────┐
│ Call-GAT │ Host-GCN │ Tree-LSTM│
│ 多层注意力 │ 双隐藏层  │ 子节点融合 │
│ 单层 RO   │ ReLU      │ root cell │
└──────────┴──────────┴──────────┘
        ↓
      MLP 融合 + GVAE 重构残差
        ↓
   ┌──────┬──────┬──────┐
   │ 预警 │ 定位 │ 诊断 │
   └──────┴──────┴──────┘
```

- **数据管道**：分布式轨迹 + 物理节点指标按时间窗口和节点名称对齐；特征侧保留接口语义、状态码、深度、位置等离散编码 + 时延/资源利用率稳健统计
- **结构侧**：调用图（Call-GAT）+ 主机共址图（Host-GCN）+ 调用树（Tree-LSTM）三类互补结构
- **域内校准 + 跨域注意力融合** 得到统一系统表示
- **双路径**：表示一方面馈送监督分类头（细粒度多类识别），另一方面进入图变分自编码器形成无监督重构通路 → 预警与定位共享同一套潜变量与残差证据
- **故障类型覆盖**：服务级（network delay / code error / network loss / cpu stress / network corrupt / memory stress / dns error）+ 主机级（node memory stress / node cpu stress / node disk fill）

---

## 7. 项目要求的 MCP 服务封装

中期报告明确："通过 MCP 服务形式实现工具化封装"——这是项目的工程交付要求。各板块的 MCP 接口规范如下：

| 板块 | MCP 服务名 | 函数清单 |
|---|---|---|
| 日志单模态 | `drain_mcp` | learn_templates_from_file / learn_template_from_single_log / match_template_for_single_log / match_templates_for_file / model_stats / list_clusters / add_masking / remove_masking / list_masking / load_model / detect_anomaly / detect_anomaly_from_file / get_model_info |
| 指标单模态 | `metrics_mcp` | preprocess_metrics / train_metrics_anomaly / test_metrics_anomaly / detect_metrics_anomaly / locate_metrics_anomaly / get_metrics_config / update_metrics_config |
| **轨迹单模态** | **`trace_tools`** | **preprocess_aiops_sv / train_aiops_sv / test_aiops_sv（wzc）** + **preprocess_trust_rca / train_trust_rca / test_trust_rca（linyu）** |
| 日志+指标融合 | `metrics_logs_mcp` | preprocess_metrics_logs / train_metrics_logs_fusion / test_metrics_logs_fusion / detect_metrics_logs_anomaly / locate_metrics_logs_anomaly / get_metrics_logs_config / update_metrics_logs_config |
| **轨迹+拓扑融合** | **`trace_topo_tools`** | **preprocess_aiops_svnd / train_aiops_svnd / test_aiops_svnd / train_tracerca / test_tracerca / get_gtrace_config / update_gtrace_config（全部 wzc）** |
| 五种全融合 | `opsaug_mcp` | OpsAug 工具层 + 内嵌表征学习模型 |

⚠️ **重要**：**本仓库 `project_3_algo_tools/` 内不存在 trace 相关 MCP 代码**（仅 OpsAug 团队的 `mcp_opsaug_server.py`）。trace 与 trace+topo 的 MCP 工作由 wzc 在独立仓库 **https://github.com/OraingeWZC/Trace_mcp** 维护。上表是中期报告对外公布的接口规范，具体实现要去 Trace_mcp 仓库看。

### 7.1 师门内部 baseline 资源（TADBench）

`/Users/wzc/Project/bk/TADBench/` 提供 7 个 trace-only baseline + 5 个公开数据集（TrainTicket / GAIA / AIOps2020 / AIOps2022 / AIOps2023）。

**关键身份**：TADBench 论文（IEEE TSC, 2025, *"A Comprehensive Benchmark and Empirical Study of Trace Anomaly Detection"* by Sun, Zhang, Pei, ..., **Dan Pei**）的作者之一**裴丹是本课题 PI**——所以 TADBench 不是外部第三方对照，而是**师门内部成果**。

**重大叙事价值**：论文末尾的 *"Future Research Directions"* 明确写道，未来 trace AD 方向应集成 TraceVAE 的 Bernoulli-Categorical Scaling / Node Count Normalization / Gaussian Std-Limit + GTrace 的层次化图编码架构——而linyu TrUST 的方法路线恰好沿这一方向开展（详见中期报告 3.3 节）。**注意**：TADBench 论文与 TrUST 同期独立开展，TrUST 不是"按论文呼吁实现"的工作，而是与论文呼吁方向**自然趋同**的后续探索。论文谱系：TraceVAE (WWW'23) → GTrace (FSE'23) → TrUST (本课题)。

**应用场景**：
- 指标 3.2 横向对照（TrUST vs TraceVAE / GTrace / PUTraceAD）
- 指标 3.4 消融叙事（关 Host 通道的 TraTopoRca vs trace-only SOTA）
- 数据集补充（AIOps2020 最小 74MB，优先验证）
- 论文/结题叙事 framing（直接引用 TADBench 决策树 Fig. 18）

详见 `TADBench整合方案.md`。

---

## 8. 数据契约对齐（来自检测报告）

### 8.1 轨迹 CSV（YS02-007/008/009 + YS02-013/014/015 共用）
固定列：
```
trace_id, span_id, parent_span_id, service_name,
duration, start_time, operation_name, status_code
```

### 8.2 K8s 节点指标 CSV（YS02-013/014/015 额外需要）
固定列：
```
fault_type, instance_id, timestamp,
node_cpu_usage_rate, node_memory_usage_rate, node_disk_usage_rate
```

### 8.3 GroundTruth 输入
- **日志类**（jsonl）：`time_range_start, time_range_end, root_cause_entities, root_cause_types`
- **指标类**（CSV）：`incident_id, injection_time, fault_time_range, fault_type_id, root_cause_type`

注：你当前代码使用 16 列 trace CSV（CamelCase 列名 `TraceID`、`SpanId` 等），与检测报告里的 snake_case 不完全一致。检测时显然有适配层处理了——保持现状即可，但交接给师弟时要说明。

---

## 9. 测试环境（来自检测报告 第 3-5 页）

```
测试数据来源：阿里云原生测试环境
工具运行环境：
  管理节点  Dell PowerEdge R420  Intel Xeon E5-2420  31GiB    Debian 11
  计算节点  Dell PowerEdge R740  Intel Xeon E5-2680v4 251GiB  Ubuntu 20.04
  存储节点  Dell PowerEdge R720/R730（多台）           Debian 9
  客户端    MacBook Air M4 2025                        macOS Tahoe 26.3.1
  交换机    Cisco SG500-28（24×千兆，72Gbps）
```

依据标准：
- 《国家重点研发计划项目任务书》
- 《国家重点研发计划课题测试方案》

---

## 10. wzc 在项目地图上的具体位置

```
                       国家重点研发计划 2024YFB4505903
                                  │
              ┌───────────────────┼─────────────────────┐
              │                   │                     │
        研究内容 1            研究内容 2            研究内容 3
        多模态数据增强       多模态时空推理       强泛化运维能力
                                                       │
                  ┌────────┬────────┼────────┬─────────┐
              指标 3.1  指标 3.2  指标 3.3  指标 3.4  指标 3.5
                日/指    轨迹     日+指融合  轨迹+拓扑  五全融合
                                  │                │
                              ┌───┴───┐       ┌────┴────┐
                              │       │       │  wzc 全责│
                          AD/RCA    CLS       AD/RCA/CLS │
                          linyu      wzc       trace_topo │
                          TrUST  trace_sv_diag           │
                                                          │
                                              赛西已认证（YS02-013/014/015）

★ wzc 的 4 个直接交付物：
   ① trace_tools/trace_sv_diag （指标 3.2 / 任务 CLS / YS02-009）
   ② trace_topo_tools/TraTopoRca （指标 3.4 / 任务 AD+RCA / YS02-013, 014）
   ③ trace_topo_tools/trace_svnd_diag （指标 3.4 / 任务 CLS / YS02-015）
   ④ trace_tools/dataprocess + trace_topo_tools/dataprocess （数据获取层）

linyu的交付物（不在 wzc 范围）：
   ⑤ trace_tools/TrUST （指标 3.2 / 任务 AD+RCA / YS02-007, 008）
```

---

## 11. 与其他子团队的接口

| 接口 | 你 → 别人 | 别人 → 你 |
|---|---|---|
| 数据 | 你输出的 trace + node metric 数据可作为 OpsAug 五模态融合的轨迹/拓扑通道输入 | OpsAug 团队拿到的 GT CSV 与你共用 |
| MCP | wzc 在独立仓库 https://github.com/OraingeWZC/Trace_mcp 维护的 `trace_tools` / `trace_topo_tools` MCP server 供 OpsAug `pipeline.py` 调用 | OpsAug 提供 `opsaug_mcp` 作为五模态调度入口 |
| 评测 | 你提供 ckpt + stats + keep_types，使 OpsAug 端能加载推理 | OpsAug 拿你的得分接入晚融合 |

**默认假设**：trace 与 trace+topo 两块的 ckpt / 数据 schema 由 wzc 维护稳定；如需变更需通知 OpsAug 团队。

---

## 12. 缺口清单（按优先级）

> ⚠️ 注意：因为团队已有自己的集成测试方案且通过赛西认证，原"必须补 evaluate_trace_*.py" 的红色优先级已降级。新的关键缺口聚焦在"项目工程化交付"与"中期到结题之间的优化"。

### 🔴 关键

1. **MCP 服务封装**（独立仓库进行中，**不在本仓库**）
   - 中期报告明确要求所有工具通过 MCP 服务暴露。
   - **代码位置**：https://github.com/OraingeWZC/Trace_mcp（wzc 个人仓库维护）。
   - 待办（在 Trace_mcp 仓库内）：
     - `trace_tools` MCP server（封装 wzc sv 三脚本 + linyu 的 trust 三脚本）
     - `trace_topo_tools` MCP server（封装 wzc svnd 三脚本 + tracerca 两脚本 + gtrace 配置读写）
   - 实现风格参考本仓库的 `mcp_opsaug_server.py`（FastMCP stdio）。

2. **项目验收数据集的范式对齐**
   - 详见 `范式对比与改造思路.md`：训练集（0305-0315）/ 测试集（0316-0325）应作为时段切分。
   - 现状：`split_and_convert_csv` 7:1:2、`split_by_problem_id` 内部时间切——验收时 4000~12000 条评测数据其实是从 CCF AIOps 切的，不是按 GT CSV 时段。

### 🟡 中等

3. **trace 板块 RCA 单点缺失（指标 3.2 已经被 TrUST 覆盖）**
   - 实际上指标 3.2 的 RCA 由linyu TrUST 完成（YS02-008 已认证），所以没有真实缺口。
   - 仅在五模态融合（指标 3.5）需要更细粒度对齐时才需要再考虑。

4. **训练集 / 测试集分布偏移**
   - 训练集几乎全 `service` 类故障，测试集混 `service/node/pod`。
   - `fault_data_fetch.py` 当前显式跳过 pod，需要决定是否补齐。

5. **数据获取脚本配置化**
   - 时间窗仍写死在 `scripts/trace_topo_tool.sh`，建议从 GT CSV 自动推导。
   - `csv-db.py`（trace_tools）默认路径 `/mnt/sdb/zly/...` 需要适配新机器。

6. **host_state_dim 解耦**
   - `model_svnd.py` 写死 24，padding/truncate fallback 静默掩盖维度错误。

### 🟢 锦上添花

7. **方法论文档归档**
   - `docs/` 下补：
     - `轨迹单模态故障预警/定位/诊断测试方案.md`（与linyu合写）
     - `轨迹拓扑融合故障预警/定位/诊断测试方案.md`
   - 结构对齐已有的 `指标工具测试方案.md` / `指标日志融合工具测试方案.md`。

8. **基线对照实验**
   - 唯一同维度对手：ART/OpsAug 家族（`ART_baseline/` / `opsaug_tools_v2/`）。
   - 在同一份测试集上跑：`evaluate_opsaug_diag.py --modalities trace,metric,log`，输出 `results/trace_topo_runs_summary.md`。

---

## 13. 给师弟的阅读顺序

1. **本文档** → 课题大局观、5 项指标结构、wzc 的位置
2. `交接文档.md` → 实操入口、目录结构、关键坑位
3. `范式对比与改造思路.md` → 评测范式细节、可选优化路径
4. 中期报告（`preview_12100000400000624D--2024YFB4505903-02.pdf`）3.3 节、3.5 节 → 算法原文
5. 检测报告（`检测报告-新.pdf`）YS02-009/013/014/015 → 实测命令与结果
6. `metric_tools/README.md` + `evaluate_metric_tools.py` → 兄弟模块怎么做"逐 case 评测"，可以借鉴的写法
7. 项目根 `README.md` → 各板块 baseline 命令清单
8. `TADBench整合方案.md` → 外部 trace baseline 库（7 个 trace AD baseline + 4 个公开数据集）的整合路径
